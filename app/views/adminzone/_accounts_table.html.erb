<table class="table table-hover table-stripped table-dark">
  <thead>
    <th>Cuenta</th>
    <th>Número</th>
    <% @line_statuses.each do |ls| %>
    <th><%= ls %></th>
    <% end %>
    <th>Opciones</th>
  </thead>
  <tbody>
    <% @accounts.each do |a| %>
    <tr>
      <td class="align-middle"><%= a.get_name %></td>
      <td class="align-middle"><%= a.get_number %></td>

      <% @line_statuses.each do |ls| %>
      <td class="align-middle"><%=  %></td>
      <% end %>

      <td class="align-middle"><%=  %></td>
    </tr>
    <% end %>
  </tbody>
</table>
