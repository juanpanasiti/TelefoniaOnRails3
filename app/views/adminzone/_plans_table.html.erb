<table class="table table-hover table-stripped table-dark">
  <thead>
    <th>Nombre</th>
    <th>Código</th>
    <% @accounts.each do |a| %>
    <th><%= a.get_name %></th>
    <% end %>
    <th>Opciones</th>
  </thead>
  <tbody>
    <% @plans.each do |a| %>
    <tr>
      <td class="align-middle"><%= a.get_name %></td>
      <td class="align-middle"><%= a.get_code_name %></td>

      <% @accounts.each do |ls| %>
      <td class="align-middle">#</td>
      <% end %>

      <td class="align-middle"><%=  %></td>
    </tr>
    <% end %>
  </tbody>
</table>
