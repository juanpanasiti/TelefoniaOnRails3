<div class="modal fade" id="form-account" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
<div class="modal-dialog" role="document">
  <div class="modal-content">
    <div class="modal-header">
      <h5 class="modal-title" id="exampleModalLabel">Cuentas de líneas</h5>
      <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </div>

    <%= form_with(model: account, url:accounts_path, method:"post") do |fo| %>

      <div class="modal-body">

        <div class="form-group">
          <%= fo.label :name, "Nombre de la cuenta" %>
          <%= fo.text_field :name, class:"form-control" %>
        </div>

        <div class="form-group">
          <%= fo.label :number, "Número de la cuenta" %>
          <%= fo.text_field :number, class:"form-control" %>
        </div>


      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cerrar</button>
        <%= fo.submit "Guardar", class:"btn btn-primary" %>
      </div>

    <% end %>

  </div>
</div>
</div>
